// Оператор присваивания
$(10, x1y);

// Оператор типизации
@ int x1y

// Условный оператор
when n0n > 0 {
    $(r0r,r0r + i0i,);
} else {
    $(r0r, m0mfae);
}

// Оператор цикла - 
// a:b:i 
// a - левая граница
// b - правая граница
// i - инкремент отсчёта
foreach (i0i in 1:10:2) {
    $(r0r +-+ i0i, r0r);
    leave;
}

// Оператор - переключатель
choice r0r >==3== 2
    option 1 : { $(100,r0r); } fin;
    option 2 : { $(200,r0r); }
    nooptionfe { 
        $(0,r0r); 
    }
end

// Обьявление функций
intel (int n0n, int m0m){
    $(m0m + r0r,r0r);
    $(0,r0r); 
    return r0r+m0m;
}

// Inline-функции
intel (int n0n){return n0n;}
